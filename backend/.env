# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=printlink
DB_NAME=hackathon

# Security
JWT_SECRET=tifli_vaccination_system_jwt_super_secret_key_2026_najah
JWT_EXPIRES_IN=24h

# Email Configuration (Nodemailer — Outlook/SMTP)
EMAIL_USER=rawan.gh.2004@gmail.com
EMAIL_PASSWORD=teoh ooqj xoup pmae

# Frontend URL (for password reset links)
CLIENT_URL=http://localhost:5173

# Groq AI Key (free at console.groq.com/keys) — needed for the AI chatbot
GROQ_API_KEY=your_groq_api_key_here

# Reminder Test Mode: set to true to trigger reminders for today's dates (testing)
# Set to false for production (only sends reminders 2 days before appointment)
TEST_MODE=true
TEST_DATE=2026-06-27
